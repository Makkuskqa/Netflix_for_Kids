# SQL VIEWS

# 1) Connect to the database you created in "sql_relations"

# 2) Create a VIEW which contains a JOIN of the 2 tables "GAME_OF_THRONES_HOUSES" and "GAME_OF_THRONES_CHARACTERS" and a WHERE condition

# 3) Now Query the VIEW
